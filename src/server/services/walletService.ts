import { PrismaClient, User } from "../../../generated/prisma/client";
import { Context } from "../context";

export const walletService = {
    getData: async ({ ctx}: {ctx:Context}) => {

    },
    updateData:async ({ ctx}: {ctx:Context}) => {

    },
}